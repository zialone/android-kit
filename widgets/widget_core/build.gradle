deps.gradleConfig.simpleModule(project)

deps.gradleConfig.simpleWHRouter(project, [includeCompiler: false])

deps.gradleConfig.simpleEvVariable(project)

deps.gradleConfig.simpleMavenPublishing(project)

android {
    buildTypes {
        debug {
            buildConfigField("String", "EV_VARIANT", "\"dev\"")
        }
        release {
            buildConfigField("String", "EV_VARIANT", "\"release\"")
        }
    }
}

dependencies {
    implementation depsApp.vendor_config
    implementation depsApp.vendor_utils
    implementation depsApp.vendor_log

    implementation deps.libs.startupRuntime
}